apiVersion: v1
kind: Service
metadata:
  name: aks-app-frontend
  namespace: aks-aap
spec:
  type: ClusterIP

  selector:
    app: aks-app-frontend

  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP